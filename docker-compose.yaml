version: '3'

services:
  db:
    image: postgres:15
    restart: always
    ports:
      - '5432:5432'

    environment:
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_NAME: ${POSTGRES_NAME}

    volumes:
      - ./postgres:/var/lib/postgresql/data
